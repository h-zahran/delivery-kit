# Changelog

Each plugin in this repository keeps its own changelog, so that the version
gates can compare a manifest against the entries that belong to it. A single
file with interleaved headings would make "the current version" a question
about release order.

- [handoff](handoff/CHANGELOG.md)
