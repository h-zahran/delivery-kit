<!-- Sync Impact Report: this comment is opened and never closed, on purpose.

## I. A check that cannot fail is not a check

Prove every verification red before trusting it green. A block whose last
command is a cleanup can only ever exit zero.

## II. Measure rather than reason

A claim about behaviour is worth exactly what its measurement is worth.
