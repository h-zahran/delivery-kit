---
name: speckit-clarify
description: Fixture stub — exists so pre-flight's invocation-form detection has a hyphen-skill install to find. Never invoked.
---

Fixture stub. The detector checks this path's existence and nothing else.
